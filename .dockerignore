node_modules
npm-debug.log
.env
.env.example
.git
.github
# The separator sidecar builds from its own context (services/separator).
# The API image never copies it, so it should not be shipped to the daemon.
services
.gitignore
coverage
README.md
Dockerfile
docker-compose.yml
.dockerignore
